diff --git a/src/gourdon/AC.cpp b/src/gourdon/AC.cpp
index 023a585..bb47839 100644
--- a/src/gourdon/AC.cpp
+++ b/src/gourdon/AC.cpp
@@ -518,6 +518,13 @@ T AC_OpenMP_default(T x,
   #include <cpu_supports_arm_sve.hpp>
 #endif
 
+#if defined(ENABLE_AVX512_VPOPCNT)
+  #include "AC_avx512.hpp"
+#elif defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  #include "AC_avx512.hpp"
+  #include <cpu_supports_avx512_vpopcnt.hpp>
+#endif
+
 namespace {
 
 /// Runtime dispatch to highly optimized SIMD algorithm if
@@ -528,11 +535,17 @@ T AC_OpenMP(T x, Args&&... args)
 {
   #if defined(ENABLE_ARM_SVE)
     return AC_OpenMP_arm_sve(x, std::forward<Args>(args)...);
+  #elif defined(ENABLE_AVX512_VPOPCNT)
+    return AC_OpenMP_avx512(x, std::forward<Args>(args)...);
   #else
     #if defined(ENABLE_MULTIARCH_ARM_SVE)
       if (cpu_supports_sve)
         return AC_OpenMP_arm_sve(x, std::forward<Args>(args)...);
     #endif
+    #if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+      if (cpu_supports_avx512_vpopcnt)
+        return AC_OpenMP_avx512(x, std::forward<Args>(args)...);
+    #endif
     #if defined(ENABLE_LIBDIVIDE)
       return AC_OpenMP_libdivide(x, std::forward<Args>(args)...);
     #else
@@ -545,11 +558,17 @@ string_view_t AC_algo_name()
 {
   #if defined(ENABLE_ARM_SVE)
     return "Algorithm: ARM SVE";
+  #elif defined(ENABLE_AVX512_VPOPCNT)
+    return "Algorithm: AVX512";
   #else
     #if defined(ENABLE_MULTIARCH_ARM_SVE)
       if (cpu_supports_sve)
         return "Algorithm: ARM SVE";
     #endif
+    #if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+      if (cpu_supports_avx512_vpopcnt)
+        return "Algorithm: AVX512";
+    #endif
     #if defined(ENABLE_LIBDIVIDE)
       return "Algorithm: libdivide";
     #else
diff --git a/src/gourdon/AC_avx512.hpp b/src/gourdon/AC_avx512.hpp
new file mode 100644
index 0000000..f7dfb66
--- /dev/null
+++ b/src/gourdon/AC_avx512.hpp
@@ -0,0 +1,494 @@
+///
+/// @file  AC_avx512.hpp
+/// @brief AVX512 vector division implementation of the A + C
+///        formulas in Xavier Gourdon's prime counting algorithm.
+///
+/// Copyright (C) 2026 Kim Walisch, <kim.walisch@gmail.com>
+///
+/// This file is distributed under the BSD License. See the COPYING
+/// file in the top level directory.
+///
+
+#ifndef AC_AVX512_HPP
+#define AC_AVX512_HPP
+
+#include <immintrin.h>
+#include <fast_div.hpp>
+
+namespace {
+
+using namespace primecount;
+
+template <typename T, int MULTIPLIER, typename XP, typename Primes>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+ALWAYS_INLINE T sum_pi_avx512(XP xp,
+                              uint64_t i,
+                              uint64_t last,
+                              uint64_t b,
+                              const Primes& primes,
+                              const SegmentedPiTable& segmentedPi)
+{
+  if (i > last)
+    return 0;
+
+  T sum = 0;
+  uint64_t size = last - i + 1;
+
+  if (xp < (1ULL << 53))
+  {
+    __m512d v_xp = _mm512_set1_pd((double)xp);
+
+    if constexpr (sizeof(primes[0]) == 4)
+    {
+      for (; i + 15 <= last; i += 16)
+      {
+        __m256i v_p32_0 = _mm256_loadu_si256((const __m256i*)&primes[i]);
+        __m256i v_p32_1 = _mm256_loadu_si256((const __m256i*)&primes[i + 8]);
+        __m512d v_p_dbl0 = _mm512_cvtepu32_pd(v_p32_0);
+        __m512d v_p_dbl1 = _mm512_cvtepu32_pd(v_p32_1);
+        __m512d v_div0 = _mm512_div_pd(v_xp, v_p_dbl0);
+        __m512d v_div1 = _mm512_div_pd(v_xp, v_p_dbl1);
+        __m512i v_q0 = _mm512_cvttpd_epu64(v_div0);
+        __m512i v_q1 = _mm512_cvttpd_epu64(v_div1);
+
+        uint64_t q0[8], q1[8];
+        _mm512_storeu_si512((void*)q0, v_q0);
+        _mm512_storeu_si512((void*)q1, v_q1);
+
+        sum += segmentedPi[q0[0]] + segmentedPi[q0[1]] +
+               segmentedPi[q0[2]] + segmentedPi[q0[3]] +
+               segmentedPi[q0[4]] + segmentedPi[q0[5]] +
+               segmentedPi[q0[6]] + segmentedPi[q0[7]] +
+               segmentedPi[q1[0]] + segmentedPi[q1[1]] +
+               segmentedPi[q1[2]] + segmentedPi[q1[3]] +
+               segmentedPi[q1[4]] + segmentedPi[q1[5]] +
+               segmentedPi[q1[6]] + segmentedPi[q1[7]];
+      }
+
+      for (; i + 7 <= last; i += 8)
+      {
+        __m256i v_p32 = _mm256_loadu_si256((const __m256i*)&primes[i]);
+        __m512d v_p_dbl = _mm512_cvtepu32_pd(v_p32);
+        __m512d v_div = _mm512_div_pd(v_xp, v_p_dbl);
+        __m512i v_q = _mm512_cvttpd_epu64(v_div);
+
+        uint64_t q[8];
+        _mm512_storeu_si512((void*)q, v_q);
+
+        sum += segmentedPi[q[0]] + segmentedPi[q[1]] +
+               segmentedPi[q[2]] + segmentedPi[q[3]] +
+               segmentedPi[q[4]] + segmentedPi[q[5]] +
+               segmentedPi[q[6]] + segmentedPi[q[7]];
+      }
+    }
+    else
+    {
+      for (; i + 15 <= last; i += 16)
+      {
+        __m512i v_p64_0 = _mm512_loadu_si512((const void*)&primes[i]);
+        __m512i v_p64_1 = _mm512_loadu_si512((const void*)&primes[i + 8]);
+        __m512d v_p_dbl0 = _mm512_cvtepi64_pd(v_p64_0);
+        __m512d v_p_dbl1 = _mm512_cvtepi64_pd(v_p64_1);
+        __m512d v_div0 = _mm512_div_pd(v_xp, v_p_dbl0);
+        __m512d v_div1 = _mm512_div_pd(v_xp, v_p_dbl1);
+        __m512i v_q0 = _mm512_cvttpd_epu64(v_div0);
+        __m512i v_q1 = _mm512_cvttpd_epu64(v_div1);
+
+        uint64_t q0[8], q1[8];
+        _mm512_storeu_si512((void*)q0, v_q0);
+        _mm512_storeu_si512((void*)q1, v_q1);
+
+        sum += segmentedPi[q0[0]] + segmentedPi[q0[1]] +
+               segmentedPi[q0[2]] + segmentedPi[q0[3]] +
+               segmentedPi[q0[4]] + segmentedPi[q0[5]] +
+               segmentedPi[q0[6]] + segmentedPi[q0[7]] +
+               segmentedPi[q1[0]] + segmentedPi[q1[1]] +
+               segmentedPi[q1[2]] + segmentedPi[q1[3]] +
+               segmentedPi[q1[4]] + segmentedPi[q1[5]] +
+               segmentedPi[q1[6]] + segmentedPi[q1[7]];
+      }
+
+      for (; i + 7 <= last; i += 8)
+      {
+        __m512i v_p64 = _mm512_loadu_si512((const void*)&primes[i]);
+        __m512d v_p_dbl = _mm512_cvtepi64_pd(v_p64);
+        __m512d v_div = _mm512_div_pd(v_xp, v_p_dbl);
+        __m512i v_q = _mm512_cvttpd_epu64(v_div);
+
+        uint64_t q[8];
+        _mm512_storeu_si512((void*)q, v_q);
+
+        sum += segmentedPi[q[0]] + segmentedPi[q[1]] +
+               segmentedPi[q[2]] + segmentedPi[q[3]] +
+               segmentedPi[q[4]] + segmentedPi[q[5]] +
+               segmentedPi[q[6]] + segmentedPi[q[7]];
+      }
+    }
+  }
+
+  for (; i <= last; i++)
+  {
+    uint64_t quotient = fast_div64(xp, primes[i]);
+    sum += segmentedPi[quotient];
+  }
+
+  return sum * MULTIPLIER + size * 2 - size * T(b);
+}
+
+/// Compute the A formula using AVX512.
+/// pi[x_star] < b <= pi[x^(1/3)]
+/// x / (primes[b] * primes[i]) < x^(1/2)
+///
+template <typename T, typename XP, typename Primes>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+T A_avx512(T xlow,
+            T xhigh,
+            XP xp,
+            uint64_t y,
+            uint64_t b,
+            const Primes& primes,
+            const PiTable& pi,
+            const SegmentedPiTable& segmentedPi)
+{
+  T sum = 0;
+
+  uint64_t prime = primes[b];
+  uint64_t sqrt_xp = isqrt(xp);
+  uint64_t min_2nd_prime = min(xhigh / prime, sqrt_xp);
+  uint64_t max_2nd_prime = min(xlow / prime, sqrt_xp);
+  uint64_t i = pi[max(prime, min_2nd_prime)] + 1;
+  uint64_t max_i1 = pi[min(xp / y, max_2nd_prime)];
+  uint64_t max_i2 = pi[max_2nd_prime];
+
+  // pq = primes[b] * primes[i]
+  // x / pq >= y && low <= x / pq < high
+  sum += sum_pi_avx512<T, 1>(xp, i, max_i1, 2, primes, segmentedPi);
+  i = max(i, max_i1 + 1);
+
+  // x / pq < y && low <= x / pq < high
+  sum += sum_pi_avx512<T, 2>(xp, i, max_i2, 2, primes, segmentedPi);
+
+  return sum;
+}
+
+/// Compute the 1st part of the C formula using AVX512.
+/// pi[(x/z)^(1/3)] < b <= pi[sqrt(z)]
+/// x / (primes[b] * m) <= z
+/// low <= x / (primes[b] * m) < high
+///
+/// m is either a prime <= y or a product of 2 distinct primes.
+/// In both cases m is coprime to the first b primes, m <= z,
+/// and its largest prime factor is <= y.
+/// Since each prime factor of m is > (x / z)^(1/3) and z < sqrt(x),
+/// m cannot contain more than 2 prime factors.
+///
+template <typename T, typename XP, typename Primes>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+T C1_avx512(T xlow,
+             T xhigh,
+             XP xp,
+             uint64_t b,
+             uint64_t y,
+             uint64_t z,
+             const Primes& primes,
+             const PiTable& pi,
+             const SegmentedPiTable& segmentedPi)
+{
+  T sum = 0;
+  uint64_t prime = primes[b];
+  uint64_t max_m = min(xlow / prime, z);
+  uint64_t x_div_prime3 = fast_div64(xp, prime * prime);
+  uint64_t min_m = fast_div64(xhigh, prime);
+  min_m = max3(min_m, x_div_prime3, z / prime);
+
+  if (min_m >= max_m)
+    return 0;
+
+  // m = primes[i]
+  uint64_t max_prime = min(y, max_m);
+
+  if (min_m < max_prime)
+  {
+    uint64_t min_i = pi[min_m] + 1;
+    uint64_t max_i = pi[max_prime];
+    sum -= sum_pi_avx512<T, 1>(xp, min_i, max_i, b, primes, segmentedPi);
+  }
+
+  // m = primes[i] * primes[j]
+  uint64_t max_q = min(y, isqrt(max_m));
+  uint64_t min_q = max(prime, min_m / y);
+
+  if (min_q < max_q)
+  {
+    uint64_t min_q_i = pi[min_q] + 1;
+    uint64_t max_q_i = pi[max_q];
+
+    NO_UNROLL_LOOP
+    for (uint64_t i = min_q_i; i <= max_q_i; i++)
+    {
+      uint64_t q = primes[i];
+      uint64_t min_r = max(q, min_m / q);
+      uint64_t max_r = min(y, max_m / q);
+
+      if (min_r >= max_r)
+        continue;
+
+      uint64_t min_j = pi[min_r] + 1;
+      uint64_t max_j = pi[max_r];
+      XP xpq = xp / q;
+
+      if (xpq <= pstd::numeric_limits<uint64_t>::max())
+        sum += sum_pi_avx512<T, 1>(uint64_t(xpq), min_j, max_j, b, primes, segmentedPi);
+      else
+        sum += sum_pi_avx512<T, 1>(xpq, min_j, max_j, b, primes, segmentedPi);
+    }
+  }
+
+  return sum;
+}
+
+/// Compute the 2nd part of the C formula.
+/// C2() computes the clustered and sparse easy leaves of the C
+/// formula for which the second factor is necessarily prime.
+/// pi[sqrt(z)] < b <= pi[x_star]
+/// x / (primes[b] * primes[i]) < x^(1/2)
+///
+template <typename T, typename XP, typename Primes>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+T C2_avx512(T xlow,
+             T xhigh,
+             XP xp,
+             uint64_t y,
+             uint64_t b,
+             uint64_t pi_y,
+             uint64_t max_clustered_global,
+             const Primes& primes,
+             const PiTable& pi,
+             const SegmentedPiTable& segmentedPi)
+{
+  uint64_t prime = primes[b];
+  uint64_t max_m = min3(xlow / prime, xp / prime, y);
+  uint64_t x_div_prime3 = fast_div64(xp, prime * prime);
+  uint64_t xhigh_div_prime = fast_div64(xhigh, prime);
+  uint64_t min_m = max3(xhigh_div_prime, x_div_prime3, prime);
+
+  if (min_m >= max_m)
+    return 0;
+
+  uint64_t pi_min_m = pi[min_m];
+  uint64_t sqrt_xp = isqrt(xp);
+  uint64_t min_clustered = in_between(min_m, sqrt_xp, max_m);
+  uint64_t pi_min_clustered = pi[min_clustered];
+  uint64_t min_clustered_global = max3(x_div_prime3, sqrt_xp, prime);
+  min_clustered_global = min(min_clustered_global, y);
+  uint64_t pi_conj_lo = pi_min_m;
+  uint64_t pi_conj_hi = pi_min_m;
+  uint64_t i = pi_min_m + 1;
+
+  T sum = 0;
+
+  // Compute the boundary correction once
+  if (max_m >= max_clustered_global &&
+      pi_y > pi_min_clustered)
+  {
+    uint64_t q_lo = fast_div64(xp, max_clustered_global);
+    uint64_t q_hi = fast_div64(xp, min_clustered_global + 1);
+    uint64_t pi_min_clustered_global = pi[min_clustered_global];
+    sum += T(pi[q_lo]) * pi_y - T(pi[q_hi]) * pi_min_clustered_global;
+    sum -= T(b - 2) * (pi_y - pi_min_clustered_global);
+  }
+
+  // Reflected range: ]pi_conj_lo, pi_conj_hi]
+  if (min_clustered_global < max_clustered_global &&
+      segmentedPi.low() < max_clustered_global &&
+      segmentedPi.high() > min_clustered_global + 1)
+  {
+    uint64_t q_lo = fast_div64(xp, max_clustered_global);
+    uint64_t q_hi = fast_div64(xp, min_clustered_global + 1);
+    pi_conj_lo = max(pi[q_lo], pi_min_m);
+    pi_conj_hi = min(pi[q_hi], pi_min_clustered);
+    pi_conj_hi = max(pi_conj_hi, pi_conj_lo);
+  }
+
+  // Sparse leaves below the reflected range
+  sum += sum_pi_avx512<T, 1>(xp, i, pi_conj_lo, b, primes, segmentedPi);
+  i = pi_conj_lo + 1;
+
+  // Reflected leaves: counted once as a sparse leaf, once as a conjugate.
+  sum += sum_pi_avx512<T, 2>(xp, i, pi_conj_hi, b, primes, segmentedPi);
+  i = pi_conj_hi + 1;
+
+  // Sparse leaves above the reflected range
+  sum += sum_pi_avx512<T, 1>(xp, i, pi_min_clustered, b, primes, segmentedPi);
+
+  return sum;
+}
+
+/// Compute A + C
+template <typename T,
+          typename Primes>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+T AC_OpenMP_avx512(T x,
+                    int64_t y,
+                    int64_t z,
+                    int64_t k,
+                    int64_t x_star,
+                    const PiTable& pi,
+                    const Primes& primes,
+                    int threads,
+                    bool is_print)
+{
+  T sum = 0;
+  int64_t x13 = iroot<3>(x);
+  int64_t sqrtx = isqrt(x);
+  int64_t xy = x / y;
+  int64_t xz = x / z;
+
+  // These load balancing settings work well on my
+  // dual-socket AMD EPYC 7642 server with 192 CPU cores.
+  int64_t thread_threshold = 1000;
+  int max_threads = (int) std::pow(xz, 1 / 3.7);
+  threads = min(threads, max_threads);
+  threads = ideal_num_threads(x13, threads, thread_threshold);
+  INDETERMINATE LoadBalancerAC loadBalancer(sqrtx, y, threads, is_print);
+
+  int64_t pi_y = pi[y];
+  int64_t max_clustered_global = primes[pi_y];
+  int64_t sqrtz = isqrt(z);
+  int64_t pi_sqrtz = pi[sqrtz];
+  int64_t pi_root3_xy = pi[iroot<3>(xy)];
+  int64_t pi_root3_xz = pi[iroot<3>(xz)];
+
+  // In order to reduce the thread creation & destruction
+  // overhead we reuse the same threads throughout the
+  // entire computation. The same threads are used for:
+  //
+  // 1) Computation of the C1 formula.
+  // 2) Computation of the C2 formula.
+  // 3) Computation of the A formula.
+  //
+  #pragma omp parallel num_threads(threads) reduction(+: sum)
+  {
+    // SegmentedPiTable is accessed very frequently.
+    // In order to get good performance it is important that
+    // SegmentedPiTable fits into the CPU's cache.
+    // Hence we use a small segment_size of x^(1/4).
+    SegmentedPiTable segmentedPi;
+    ThreadDataAC thread;
+
+    // for (low = 0; low < sqrt(x); low += segment_size)
+    while (loadBalancer.get_work(thread))
+    {
+      int64_t low = thread.low;
+      int64_t segment_size = thread.segment_size;
+      int64_t limit = low + thread.segments * segment_size;
+      limit = min(limit, sqrtx);
+
+      for (; low < limit; low += segment_size)
+      {
+        // Current segment [low, high[
+        int64_t high = low + segment_size;
+        high = min(high, sqrtx);
+        segmentedPi.init(low, high, limit);
+
+        // We measure the thread computation time excluding the
+        // first expensive initialization of the segmentedPi
+        // lookup table. If the thread computation time is close
+        // to 0 then we increase the number of segments in the
+        // loadBalancer which should improve performance.
+        if (low == thread.low)
+          thread.secs = get_time();
+
+        int64_t pi_sqrt_low = pi[isqrt(low)];
+        T xlow = x / max(low, 1);
+        T xhigh = x / high;
+
+        if (low < z)
+        {
+          int64_t min_c1 = max(k, pi_root3_xz);
+          int64_t min_c1_prime = min(xz / high, sqrtz);
+          min_c1 = max3(min_c1, pi_sqrt_low, pi[min_c1_prime]) + 1;
+
+          // C1 formula: pi[(x/z)^(1/3)] < b <= pi[sqrt(z)]
+          for (int64_t b = min_c1; b <= pi_sqrtz; b++)
+          {
+            T xp = x / primes[b];
+
+            if (xp <= pstd::numeric_limits<uint64_t>::max())
+              sum -= C1_avx512(xlow, xhigh, uint64_t(xp), b, y, z, primes, pi, segmentedPi);
+            else
+              sum -= C1_avx512(xlow, xhigh, xp, b, y, z, primes, pi, segmentedPi);
+          }
+        }
+
+        int64_t min_c2 = max3(k, pi_root3_xy, pi_sqrtz);
+        int64_t min_c2_prime = min(xhigh / y, x_star);
+        min_c2 = max3(min_c2, pi_sqrt_low, pi[min_c2_prime]) + 1;
+        int64_t x_div_high2 = fast_div64(xhigh, high);
+        int64_t min_a = min(x_div_high2, x13);
+        min_a = pi[max(x_star, min_a)] + 1;
+
+        // Upper bound of A & C2 formulas:
+        // x / (p * q) >= low
+        // p * next_prime(p) <= x / low
+        // p <= sqrt(x / low)
+        int64_t sqrt_xlow = isqrt(xlow);
+        int64_t max_c2 = pi[min(sqrt_xlow, x_star)];
+        T max_c2_prime = xlow / max(max_clustered_global, 1);
+        int64_t max_c2_clustered = pi[min3(max_c2_prime, sqrt_xlow, x_star)];
+        int64_t min_c2_sparse = pi[min(x_div_high2, x_star)] + 1;
+        min_c2_sparse = max3(min_c2, min_c2_sparse, max_c2_clustered + 1);
+        int64_t max_a = pi[min(sqrt_xlow, x13)];
+
+        // C2 formula: pi[sqrt(z)] < b <= pi[x_star]
+        for (int64_t b = min_c2; b <= max_c2_clustered; b++)
+        {
+          T xp = x / primes[b];
+
+          if (xp <= pstd::numeric_limits<uint64_t>::max())
+            sum += C2_avx512(xlow, xhigh, uint64_t(xp), y, b, pi_y, max_clustered_global, primes, pi, segmentedPi);
+          else
+            sum += C2_avx512(xlow, xhigh, xp, y, b, pi_y, max_clustered_global, primes, pi, segmentedPi);
+        }
+
+        // C2 formula: pi[sqrt(z)] < b <= pi[x_star]
+        for (int64_t b = min_c2_sparse; b <= max_c2; b++)
+        {
+          T xp = x / primes[b];
+
+          if (xp <= pstd::numeric_limits<uint64_t>::max())
+            sum += C2_avx512(xlow, xhigh, uint64_t(xp), y, b, pi_y, max_clustered_global, primes, pi, segmentedPi);
+          else
+            sum += C2_avx512(xlow, xhigh, xp, y, b, pi_y, max_clustered_global, primes, pi, segmentedPi);
+        }
+
+        // A formula: pi[x_star] < b <= pi[x13]
+        for (int64_t b = min_a; b <= max_a; b++)
+        {
+          T xp = x / primes[b];
+
+          if (xp <= pstd::numeric_limits<uint64_t>::max())
+            sum += A_avx512(xlow, xhigh, uint64_t(xp), y, b, primes, pi, segmentedPi);
+          else
+            sum += A_avx512(xlow, xhigh, xp, y, b, primes, pi, segmentedPi);
+        }
+      }
+    }
+  }
+
+  return sum;
+}
+
+} // namespace
+
+#endif
diff --git a/src/gourdon/B.cpp b/src/gourdon/B.cpp
index b135c63..1494887 100644
--- a/src/gourdon/B.cpp
+++ b/src/gourdon/B.cpp
@@ -29,12 +29,19 @@
 #include <stdint.h>
 #include <algorithm>
 
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT) || defined(ENABLE_AVX512_VPOPCNT)
+  #include <immintrin.h>
+#endif
+
 using namespace primecount;
 
 namespace {
 
 /// Thread sieves [low, high[
 template <typename T>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
 T B_thread(T x,
            int64_t y,
            int64_t low,
@@ -71,7 +78,29 @@ T B_thread(T x,
 
     for (; it2.primes_[it2.size_ - 1] <= xp; it2.generate_next_primes())
       pi_xp += it2.size_ - it2.i_;
-    for (; it2.primes_[it2.i_] <= xp; it2.i_++)
+
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT) || defined(ENABLE_AVX512_VPOPCNT)
+    __m512i v_xp = _mm512_set1_epi64(xp);
+    while (it2.i_ + 7 < it2.size_)
+    {
+      __m512i v_p = _mm512_loadu_si512((const void*)&it2.primes_[it2.i_]);
+      __mmask8 m = _mm512_cmple_epu64_mask(v_p, v_xp);
+      if (m == 0xFF)
+      {
+        it2.i_ += 8;
+        pi_xp += 8;
+      }
+      else
+      {
+        int cnt = _mm_popcnt_u32(m);
+        it2.i_ += cnt;
+        pi_xp += cnt;
+        break;
+      }
+    }
+#endif
+
+    for (; it2.i_ < it2.size_ && it2.primes_[it2.i_] <= xp; it2.i_++)
       pi_xp += 1;
 
     sum += pi_xp;
diff --git a/src/gourdon/D_avx512.hpp b/src/gourdon/D_avx512.hpp
index 459ccef..e983c73 100644
--- a/src/gourdon/D_avx512.hpp
+++ b/src/gourdon/D_avx512.hpp
@@ -128,9 +128,57 @@ ALWAYS_INLINE __m512i load_factor_tail_epi64_avx512(const uint32_t* factor_table
                                         _mm512_cvtepu32_epi64(vec));
 }
 
+template <typename XP, typename Index, std::size_t N, std::size_t M, typename FactorTable>
+#if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
+#endif
+ALWAYS_INLINE void batch_div_avx512(XP xp,
+                                    const Array<Index, N>& indexes,
+                                    Array<int64_t, M>& xpm_cache,
+                                    std::size_t m_count,
+                                    const FactorTable& factor)
+{
+  std::size_t i = 0;
+  if (xp < (1ULL << 53))
+  {
+    __m512d v_xp = _mm512_set1_pd((double)xp);
+
+    for (; i + 15 < m_count; i += 16)
+    {
+      double m0[8], m1[8];
+      for (int k = 0; k < 8; k++) m0[k] = (double)factor.to_number(indexes[i + k]);
+      for (int k = 0; k < 8; k++) m1[k] = (double)factor.to_number(indexes[i + 8 + k]);
+      __m512d v_m0 = _mm512_loadu_pd(m0);
+      __m512d v_m1 = _mm512_loadu_pd(m1);
+      __m512d v_q0 = _mm512_div_pd(v_xp, v_m0);
+      __m512d v_q1 = _mm512_div_pd(v_xp, v_m1);
+      __m512i q0 = _mm512_cvttpd_epu64(v_q0);
+      __m512i q1 = _mm512_cvttpd_epu64(v_q1);
+      _mm512_storeu_si512((void*)&xpm_cache[i], q0);
+      _mm512_storeu_si512((void*)&xpm_cache[i + 8], q1);
+    }
+
+    for (; i + 7 < m_count; i += 8)
+    {
+      double m0[8];
+      for (int k = 0; k < 8; k++) m0[k] = (double)factor.to_number(indexes[i + k]);
+      __m512d v_m0 = _mm512_loadu_pd(m0);
+      __m512d v_q0 = _mm512_div_pd(v_xp, v_m0);
+      __m512i q0 = _mm512_cvttpd_epu64(v_q0);
+      _mm512_storeu_si512((void*)&xpm_cache[i], q0);
+    }
+  }
+
+  for (; i < m_count; i++)
+  {
+    int64_t m = factor.to_number(indexes[i]);
+    xpm_cache[i] = fast_div64(xp, m);
+  }
+}
+
 template <typename T, typename Primes, typename FactorTable>
 #if defined(ENABLE_MULTIARCH_AVX512_VPOPCNT)
-  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512vpopcntdq")))
+  __attribute__ ((target ("avx512f,avx512bw,avx512vl,avx512dq,avx512vpopcntdq")))
 #endif
 T D_thread_avx512(T x,
                   int64_t x_star,
@@ -226,12 +274,7 @@ T D_thread_avx512(T x,
 
           if (m_count > max_m_count)
           {
-            // Batch calculate xp/m to improve CPU pipelining
-            for (std::size_t i = 0; i < m_count; i++)
-            {
-              int64_t m = factor.to_number(m_indexes32[i]);
-              xpm_cache[i] = fast_div64(xp, m);
-            }
+            batch_div_avx512(xp, m_indexes32, xpm_cache, m_count, factor);
 
             // Process the next few special leaves that are
             // composed of a prime and a square free number:
@@ -260,12 +303,7 @@ T D_thread_avx512(T x,
           m_count += popcnt64_native(mask);
         }
 
-        // Batch calculate xp/m to improve CPU pipelining
-        for (std::size_t i = 0; i < m_count; i++)
-        {
-          int64_t m = factor.to_number(m_indexes32[i]);
-          xpm_cache[i] = fast_div64(xp, m);
-        }
+        batch_div_avx512(xp, m_indexes32, xpm_cache, m_count, factor);
 
         // Process the last few m values
         for (std::size_t i = 0; i < m_count; i++)
@@ -293,12 +331,7 @@ T D_thread_avx512(T x,
 
           if (m_count > max_m_count)
           {
-            // Batch calculate xp/m to improve CPU pipelining
-            for (std::size_t i = 0; i < m_count; i++)
-            {
-              int64_t m = factor.to_number(m_indexes64[i]);
-              xpm_cache[i] = fast_div64(xp, m);
-            }
+            batch_div_avx512(xp, m_indexes64, xpm_cache, m_count, factor);
 
             // Process the next few special leaves that are
             // composed of a prime and a square free number:
@@ -327,12 +360,7 @@ T D_thread_avx512(T x,
           m_count += popcnt64_native(mask);
         }
 
-        // Batch calculate xp/m to improve CPU pipelining
-        for (std::size_t i = 0; i < m_count; i++)
-        {
-          int64_t m = factor.to_number(m_indexes64[i]);
-          xpm_cache[i] = fast_div64(xp, m);
-        }
+        batch_div_avx512(xp, m_indexes64, xpm_cache, m_count, factor);
 
         // Process the last few m values
         for (std::size_t i = 0; i < m_count; i++)
